# PostgreSQL — rode: docker compose up -d db
DATABASE_URL=postgresql://gymtracker:gymtracker@localhost:5432/gymtracker

# Segredo JWT (mude em produção)
JWT_SECRET=troca-em-producao

# Porta da API
PORT=3000
